import { EntityResourcePage } from '@/components/ui/EntityResourcePage';

export default function FormularioCategoriaPage() {
  return <EntityResourcePage entity="FORMULARIO_CATEGORIA" title="Formulários por categoria" description="Associe categorias aos formulários de cada paciente." />;
}
