import { EntityResourcePage } from '@/components/ui/EntityResourcePage';

export default function RespostaPage() {
  return <EntityResourcePage entity="RESPOSTA" title="Respostas" description="Administre as opções e respostas associadas às perguntas." />;
}
