import { EntityResourcePage } from '@/components/ui/EntityResourcePage';

export default function CategoriaPerguntaPage() {
  return <EntityResourcePage entity="CATEGORIA_PERGUNTA" title="Categorias por pergunta" description="Associe cada pergunta à sua categoria." />;
}
