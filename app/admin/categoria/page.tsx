import { EntityResourcePage } from '@/components/ui/EntityResourcePage';

export default function CategoriaPage() {
  return <EntityResourcePage entity="CATEGORIA" title="Categorias" description="Organize categorias de perguntas e formulários." />;
}
