import { EntityResourcePage } from '@/components/ui/EntityResourcePage';

export default function PacientePerfilPage() {
  return <EntityResourcePage entity="PACIENTE_PERFIL" title="Pacientes por perfil" description="Relacione pacientes com os perfis associados." />;
}
