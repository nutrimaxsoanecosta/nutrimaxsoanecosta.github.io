import { EntityResourcePage } from '@/components/ui/EntityResourcePage';

export default function PerfilPerguntaPage() {
  return <EntityResourcePage entity="PERFIL_PERGUNTA" title="Perfis por pergunta" description="Defina quais perguntas pertencem a cada perfil." />;
}
