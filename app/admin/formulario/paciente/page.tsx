import { EntityResourcePage } from '@/components/ui/EntityResourcePage';

export default function FormularioPacientePage() {
  return <EntityResourcePage entity="FORMULARIO" title="Formulários do paciente" description="Gerencie os formulários vinculados ao paciente." />;
}
