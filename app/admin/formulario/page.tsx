import { EntityResourcePage } from '@/components/ui/EntityResourcePage';

export default function FormularioPage() {
  return <EntityResourcePage entity="FORMULARIO" title="Formulários" description="Gerencie os formulários do sistema." />;
}
