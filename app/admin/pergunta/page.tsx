import { EntityResourcePage } from '@/components/ui/EntityResourcePage';

export default function PerguntaPage() {
  return <EntityResourcePage entity="PERGUNTA" title="Perguntas" description="Crie e edite as perguntas do questionário." />;
}
