import { EntityResourcePage } from '@/components/ui/EntityResourcePage';

export default function PerfilPage() {
  return <EntityResourcePage entity="PERFIL" title="Perfis" description="Cadastre e gerencie os perfis disponíveis." />;
}
