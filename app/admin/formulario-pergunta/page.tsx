import { EntityResourcePage } from '@/components/ui/EntityResourcePage';

export default function FormularioPerguntaPage() {
  return <EntityResourcePage entity="FORMULARIO_PERGUNTA" title="Formulários por pergunta" description="Associe perguntas aos formulários de cada paciente." />;
}
