import { EntityResourcePage } from '@/components/ui/EntityResourcePage';

export default function TipoPerguntaPage() {
  return <EntityResourcePage entity="TIPO_PERGUNTA" title="Tipos de pergunta" description="Gerencie os tipos disponíveis para as perguntas." />;
}
